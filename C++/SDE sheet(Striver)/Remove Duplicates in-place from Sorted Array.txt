//brute approach - using hashset
int removeDuplicates(int arr[],int n){
	set<int>st;
	for(int i=0;i<arr.size();i++){
		st.insert(arr[i]);
	}
	int k = set.size();
	int j=0;
	for(int x:set){
		arr[j++] = x; 
	}
	return k;
}
//Optimal solution - two pointers
int removeDuplicates(int arr,int n){
	int i=0;
	for(int j=1;j<n;j++){
		if(arr[i]!=arr[j]){
			i++;
		arr[i]=arr[j];
	}
}
 return i+1; //size of the array of the unique elements
}