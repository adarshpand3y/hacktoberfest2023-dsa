//recursive approach(DFS) - post order traversal
#include<bits/stdc++.h>
int maxDepth(TreeNode*root){
	if(root==NULL)
	return -1;
	
	int lh = maxDepth(root->left);
	int rh = maxDepth(root->right);
	
	return 1+max(lh,rh);
}